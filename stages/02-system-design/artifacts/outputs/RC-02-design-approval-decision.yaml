# =============================================================================
# RC-02 — Design Approval Decision
# Control:   RC-02 Design Approval (HUMAN REQUIRED)
# Stage:     2 — System Design
# Feature:   FEAT-0004 — Trauma-Specialised Psychologist Masterprompt
# Status:    pending — awaiting Lead Architect approval
# =============================================================================
# This control cannot be delegated to an agent. Agent has prepared the
# approval package below. Human must make the formal approval decision.
# Risk tier: MEDIUM → required authority: Lead Architect
# =============================================================================

meta:
  id: "RC-02-FEAT-0004"
  feature: "FEAT-0004"
  produced_by: "Claude Sonnet 4.6 (approval package prepared)"
  date: "2026-03-14"
  status: approved

# ── Approval Package ───────────────────────────────────────────────────────────
approval_package:
  risk_tier: medium
  required_authority: "Lead Architect (Jerry van Heerikhuize)"

  artifacts:
    - path: "stages/02-system-design/artifacts/outputs/QC-03-design-document.yaml"
      summary: >
        5-component MVC architecture (CORE_DIRECTIVES, MODEL, VIEW, RULES_ENGINE,
        CONTROLLER). All 17 requirements (11 FR + 6 NFR) mapped — 0 unmapped.
        4 trust boundaries identified and controlled. 3 minor design gaps documented
        (technique protocol selection, crisis resource localisation, language detection
        fallback) — all deferred to Stage 3 by design. Design directive compliance:
        DESIGN-01 compliant, DESIGN-02 compliant, DESIGN-03 compliant.

    - path: "stages/02-system-design/artifacts/outputs/SC-05-stride-threat-model.yaml"
      summary: >
        9 threats across 4 trust boundaries. 1 CRITICAL (T-01: crisis bypass via
        injection — mitigated by dual ABSOLUTE_RULES + SESSION_LOOP step 2 ordering).
        2 HIGH (T-02: safe-messaging override — mitigated; T-03: clinical authority
        claim — mitigated). 4 MEDIUM, 2 LOW. 0 unmitigated critical/high threats.
        0 stage blockers. Stage 3 implementation actions noted for T-01, T-02, T-03,
        T-04, T-05, T-07.

    - path: "stages/02-system-design/artifacts/outputs/AC-03-ai-component-design-review.yaml"
      summary: >
        AI component confirmed (limited-risk prompt). Model-agnostic by design.
        No data pipeline. Art. 52(1) transparency: satisfied by mandatory two-tier
        disclaimer. Art. 53 GPAI documentation: satisfied by A-SDLC artifact set.
        Art. 14 human oversight: satisfied by console commands + crisis-to-human
        escalation + Phase 1 scope limit. Minor gap ACG-01: multi-LLM safety testing
        required in Stage 4.

    - path: "stages/02-system-design/artifacts/outputs/RC-03-privacy-impact-assessment.yaml"
      summary: >
        APPLICABLE — GDPR Art. 9 special category data (mental health). Full DPIA
        not required (individual self-directed sessions, no centralised processing).
        PIA completed. 4 privacy risks assessed; all accepted with documented rationale.
        Overall privacy risk: MEDIUM — driven by LLM provider retention (structural,
        outside prompt's control; mitigated by mandatory disclosure at session open).

    - path: "stages/02-system-design/artifacts/outputs/SC-06-zero-trust-validation.yaml"
      summary: >
        Network zero-trust: NOT APPLICABLE (no network architecture). Logical trust
        posture: COMPLIANT across all 6 criteria including safety-rule non-degradability
        (enhanced scrutiny applied due to health-adjacent use case).

    - path: "stages/02-system-design/artifacts/outputs/AC-04-ai-model-governance-record.yaml"
      summary: "AC-04 NOT APPLICABLE — model-agnostic; no specific model introduced or pinned."

  agent_recommendation: approve
  agent_recommendation_rationale: >
    All 17 requirements are mapped to architectural components. No unmitigated
    blocking threats. The CRITICAL threat (T-01 crisis bypass) is mitigated through
    dual enforcement — ABSOLUTE_RULES at highest authority AND SESSION_LOOP step 2
    ordering. Applicable controls completed (QC-03, SC-05, AC-03, RC-03, SC-06).
    Not-applicable controls documented (AC-04). Three design gaps are minor and
    deferred to Stage 3 by explicit design decision. Stage 3 implementation actions
    are clearly documented for all HIGH/CRITICAL threats. Design is implementation-ready.

# ── Human Decision ─────────────────────────────────────────────────────────────
decision: approved
decided_by: "Jerry van Heerikhuize (Lead Architect)"
decision_date: "2026-03-14"
conditions: ""
rejection_reason: ""

stage_2_exit_clear: true
