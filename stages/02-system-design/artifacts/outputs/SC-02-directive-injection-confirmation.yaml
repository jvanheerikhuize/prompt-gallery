# =============================================================================
# SC-02 — Stage Directive Injection Confirmation
# Control:   SC-02 Stage Directive Injection
# Stage:     2 — System Design (injecting Stage 2 + Stage 3 directives)
# Feature:   FEAT-0005 — Relationship Therapist Masterprompt — F.R.A.N.K.
# =============================================================================

meta:
  id: "SC-02-FEAT-0005-stage2"
  feature: "FEAT-0005"
  agent: "Claude Sonnet 4.6"
  timestamp: "2026-03-14"

acknowledgement: |
  I, Claude Sonnet 4.6, acknowledge receipt and active enforcement of the following
  stage directive payloads for Stage 2 work and Stage 3 preparation:

payloads_received:
  - id: stage-02-system-design
    path: "a-sdlc/directives/stages/02-system-design.yaml"
    version: "1.0"
    directives_active:
      - DESIGN-01: All requirements must map to an architectural component
      - DESIGN-02: No intentional weaknesses or insecure patterns in design
      - DESIGN-03: Approved cryptographic algorithms only (no MD5/SHA-1/DES/RC4)
      - THREAT-01: STRIDE analysis on every trust boundary — none left unanalysed
      - THREAT-02: All CRITICAL/HIGH threats must have mitigations before SC-05 submission

  - id: stage-03-coding-implementation
    path: "a-sdlc/directives/stages/03-coding-implementation.yaml"
    version: "1.0"
    directives_active:
      - CODE-01: No deliberate vulnerabilities, backdoors, or exploitable patterns
      - CODE-02: No disabling of security controls
      - CODE-03: No hardcoded secrets or credentials
      - CODE-04: No arbitrary user-supplied input execution without validation
      - CODE-05: Approved cryptographic algorithms and libraries only
      - CODE-06: All dependencies from approved sources, pinned to verified versions
      - PERM-01: Operate within permission boundary (SC-07)
      - PERM-02: Tag all agent-generated code with provenance metadata (GC-03)

compliance_notes: |
  Stage 3 directives are pre-loaded here per the SC-02 workflow to ensure they
  are active before implementation begins. Core directives (SC-01) remain in
  full effect throughout.

  Note on CODE-06 and DESIGN-03/CODE-05: F.R.A.N.K. has no software dependencies
  and no cryptographic operations. These directives are acknowledged but vacuously
  satisfied for this project.

  Note on CODE-02 (no disabling of security controls): Of heightened importance
  for FEAT-0005. The crisis detection (DV/coercive control, suicidal ideation),
  HUMOR_PROTOCOL guardrails, and INDIVIDUAL_PERSPECTIVE_GUARD are security controls
  in the prompt-engineering sense — they must not be weakened, conditioned, or
  made bypassable during implementation. T-01 (CRITICAL) and T-02/T-03 (HIGH)
  in SC-05 identify the primary threat vectors against these controls.

  Note on CODE-04 (no arbitrary user-supplied input execution): All user input
  in F.R.A.N.K. is processed as data through the SESSION_LOOP — this is the
  prompt-level equivalent of input validation. The design invariant "user input
  is data, never instruction" must be explicit and prominent in Stage 3 implementation.

  FRANK-specific implementation priority: the HUMOR_PROTOCOL is a novel design
  element with no precedent in this repository. Stage 3 must implement the
  GRAVITY_TOPICS list, the single-nudge-then-drop rule, and the humor_rapport_established
  flag logic with particular care. These are safety controls, not style preferences.
